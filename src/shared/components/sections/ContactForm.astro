---
import Card from '../ui/Card.astro';
import Button from '../ui/Button.astro';
import IconBox from '../ui/IconBox.astro';
import { getCollection } from 'astro:content';

interface Benefit {
  icon: string;
  title: string;
  description: string;
}

interface Props {
  benefits?: Benefit[];
  class?: string;
}

const defaultBenefits: Benefit[] = [
  {
    icon: 'fa-solid fa-clock',
    title: 'Швидка відповідь',
    description: 'Відповім протягом 24 годин',
  },
  {
    icon: 'fa-solid fa-comments',
    title: 'Безкоштовна консультація',
    description: 'Обговоримо ідею безкоштовно',
  },
  {
    icon: 'fa-solid fa-file-contract',
    title: 'Прозорі умови',
    description: 'Чітка угода та терміни',
  },
];

const {
  benefits = defaultBenefits,
  class: className = '',
} = Astro.props;

const allServices = (await getCollection('services')).sort((a, b) => a.data.order - b.data.order);
---

<section class:list={['grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-start w-full', className]} aria-labelledby="contact-form-heading">
  <div class="space-y-6">
    <div class="space-y-2">
      <span class="inline-flex items-center gap-1.5 px-3.5 py-1 rounded-[10px] text-xs font-black uppercase tracking-wider bg-[#E8BA98] text-[#2A1E1E] shadow-2xs">
        <i class="fa-solid fa-paper-plane text-xs"></i>
        Зв'язок
      </span>
      <h2 id="contact-form-heading" class="text-2xl sm:text-4xl font-black text-[#2A1E1E] tracking-tight m-0">
        Напишіть мені
      </h2>
      <p class="text-sm sm:text-base text-[#623A2D]/80 font-medium leading-relaxed m-0">
        Опишіть ваш проєкт, і я відповім протягом 24 годин з пропозицією щодо термінів та вартості.
      </p>
    </div>

    <div class="space-y-3.5" role="list" aria-label="Переваги співпраці">
      {benefits.map((benefit) => (
        <div class="bg-white border border-[#623A2D]/10 rounded-[10px] p-4 sm:p-5 flex items-center gap-4 shadow-2xs hover:border-[#D09AFC] hover:shadow-md transition-all" role="listitem">
          <div class="w-10 h-10 rounded-[10px] bg-[#FAF1EC] border border-[#623A2D]/10 flex items-center justify-center text-[#2A1E1E] shrink-0">
            <i class={benefit.icon}></i>
          </div>
          <div>
            <h4 class="text-sm font-black text-[#2A1E1E] m-0">{benefit.title}</h4>
            <p class="text-xs text-[#623A2D]/75 font-medium m-0 mt-0.5">{benefit.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>

  <div class="bento-card p-6 sm:p-8 w-full shadow-sm">
    <form class="space-y-4" id="contact-form" aria-describedby="form-instructions">
      <p id="form-instructions" class="sr-only">Заповніть форму для зв'язку. Поля з позначкою * обов'язкові.</p>
      
      <div class="space-y-1.5">
        <label class="text-xs font-black text-[#2A1E1E] uppercase tracking-wider block" for="name">
          Ім'я <span class="text-red-500" aria-hidden="true">*</span>
        </label>
        <input
          class="w-full bg-[#FAF1EC]/30 border border-[#623A2D]/15 rounded-[10px] px-4 py-3 text-sm text-[#2A1E1E] outline-none focus:bg-white focus:border-[#D09AFC] focus:ring-2 focus:ring-[#D09AFC]/20 transition-all"
          type="text"
          id="name"
          name="name"
          placeholder="Ваше ім'я"
          required
          aria-required="true"
          autocomplete="name"
        />
      </div>

      <div class="space-y-1.5">
        <label class="text-xs font-black text-[#2A1E1E] uppercase tracking-wider block" for="contact">
          Зручний зв'язок <span class="text-red-500" aria-hidden="true">*</span>
        </label>
        <input
          class="w-full bg-[#FAF1EC]/30 border border-[#623A2D]/15 rounded-[10px] px-4 py-3 text-sm text-[#2A1E1E] outline-none focus:bg-white focus:border-[#D09AFC] focus:ring-2 focus:ring-[#D09AFC]/20 transition-all"
          type="text"
          id="contact"
          name="contact"
          placeholder="Telegram, email або телефон"
          required
          aria-required="true"
          autocomplete="email tel"
        />
      </div>

      <div class="space-y-1.5">
        <label id="service-label" class="text-xs font-black text-[#2A1E1E] uppercase tracking-wider block">
          Послуга <span class="text-red-500" aria-hidden="true">*</span>
        </label>
        <input type="hidden" id="service" name="service" required aria-required="true" />
        <div class="relative" id="service-dropdown">
          <button
            type="button"
            class="w-full bg-[#FAF1EC]/30 border border-[#623A2D]/15 rounded-[10px] px-4 py-3 text-sm text-left outline-none focus:bg-white focus:border-[#D09AFC] focus:ring-2 focus:ring-[#D09AFC]/20 transition-all cursor-pointer flex items-center justify-between gap-2 text-[#2A1E1E]"
            id="service-trigger"
            aria-haspopup="listbox"
            aria-expanded="false"
            aria-labelledby="service-label"
          >
            <span class="flex items-center gap-2.5" id="service-trigger-content">
              <span class="text-[#623A2D]/50 font-medium">Оберіть послугу</span>
            </span>
            <i class="fa-solid fa-chevron-down text-xs text-[#623A2D]/50 transition-transform duration-200" id="service-arrow"></i>
          </button>
          <div
            class="absolute top-full left-0 right-0 mt-1.5 bg-white border border-[#623A2D]/10 rounded-[10px] shadow-xl overflow-hidden z-50 hidden"
            id="service-options"
            role="listbox"
            aria-labelledby="service-label"
          >
            <div class="p-1.5">
              {allServices.map((service) => (
                <button
                  type="button"
                  class="service-option w-full flex items-center gap-3 px-3 py-2.5 rounded-[8px] text-sm text-left hover:bg-[#FAF1EC] transition-colors cursor-pointer"
                  data-value={service.id}
                  data-icon={service.data.icon}
                  data-title={service.data.title}
                  role="option"
                  aria-selected="false"
                >
                  <div class="w-8 h-8 rounded-[8px] bg-[#FAF1EC] border border-[#623A2D]/10 flex items-center justify-center shrink-0">
                    <i class={`${service.data.icon} text-sm text-[#2A1E1E]`}></i>
                  </div>
                  <span class="font-bold text-[#2A1E1E]">{service.data.title}</span>
                </button>
              ))}
              <button
                type="button"
                class="service-option w-full flex items-center gap-3 px-3 py-2.5 rounded-[8px] text-sm text-left hover:bg-[#FAF1EC] transition-colors cursor-pointer"
                data-value="other"
                data-icon="fa-solid fa-ellipsis"
                data-title="Інше"
                role="option"
                aria-selected="false"
              >
                <div class="w-8 h-8 rounded-[8px] bg-[#FAF1EC] border border-[#623A2D]/10 flex items-center justify-center shrink-0">
                  <i class="fa-solid fa-ellipsis text-sm text-[#2A1E1E]/60"></i>
                </div>
                <span class="font-bold text-[#2A1E1E]">Інше</span>
              </button>
            </div>
          </div>
        </div>
      </div>

      <div class="space-y-1.5">
        <label class="text-xs font-black text-[#2A1E1E] uppercase tracking-wider block" for="message">
          Опишіть проєкт <span class="text-red-500" aria-hidden="true">*</span>
        </label>
        <textarea
          class="w-full bg-[#FAF1EC]/30 border border-[#623A2D]/15 rounded-[10px] px-4 py-3 text-sm text-[#2A1E1E] outline-none focus:bg-white focus:border-[#D09AFC] focus:ring-2 focus:ring-[#D09AFC]/20 transition-all min-h-[110px] resize-y"
          id="message"
          name="message"
          placeholder="Коротко опишіть вашу ідею або задачу..."
          rows="4"
          required
          aria-required="true"
        ></textarea>
      </div>

      <button
        type="submit"
        class="pill-btn-lilac w-full !py-3.5 !text-sm flex items-center justify-center gap-2 shadow-md mt-2"
        id="submit-btn"
      >
        <span id="submit-text">Надіслати заявку</span>
        <i class="fa-solid fa-arrow-right text-xs" id="submit-icon" aria-hidden="true"></i>
        <svg class="animate-spin h-4 w-4 hidden" id="submit-spinner" viewBox="0 0 24 24" aria-hidden="true">
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4" fill="none" />
          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />
        </svg>
      </button>

      <div id="form-status" class="text-sm text-center font-bold" role="status" aria-live="polite"></div>
    </form>
  </div>
</section>

<script>
  document.addEventListener('astro:page-load', () => {
    const form = document.getElementById('contact-form') as HTMLFormElement | null;
    const submitBtn = document.getElementById('submit-btn');
    const submitText = document.getElementById('submit-text');
    const submitIcon = document.getElementById('submit-icon');
    const submitSpinner = document.getElementById('submit-spinner');
    const formStatus = document.getElementById('form-status');

    // Custom dropdown
    const dropdown = document.getElementById('service-dropdown');
    const trigger = document.getElementById('service-trigger');
    const triggerContent = document.getElementById('service-trigger-content');
    const optionsPanel = document.getElementById('service-options');
    const arrow = document.getElementById('service-arrow');
    const hiddenInput = document.getElementById('service') as HTMLInputElement;
    const options = document.querySelectorAll('.service-option');

    if (dropdown && trigger && optionsPanel && arrow && hiddenInput) {
      trigger.addEventListener('click', () => {
        const isOpen = !optionsPanel.classList.contains('hidden');
        optionsPanel.classList.toggle('hidden');
        arrow.classList.toggle('rotate-180');
        trigger.classList.toggle('border-cobalt-pop');
        trigger.setAttribute('aria-expanded', String(!isOpen));
      });

      options.forEach((option) => {
        option.addEventListener('click', () => {
          const value = option.getAttribute('data-value') || '';
          const icon = option.getAttribute('data-icon') || '';
          const title = option.getAttribute('data-title') || '';

          hiddenInput.value = value;
          triggerContent.innerHTML = `
            <div class="w-8 h-8 rounded-lg bg-kofi-blue/40 flex items-center justify-center shrink-0">
              <i class="${icon} text-sm" style="color: var(--color-cobalt-pop, #3b5bdb);"></i>
            </div>
            <span class="font-medium text-ink-black">${title}</span>
          `;

          optionsPanel.classList.add('hidden');
          arrow.classList.remove('rotate-180');
          trigger.classList.remove('border-cobalt-pop');
          trigger.setAttribute('aria-expanded', 'false');
          hiddenInput.removeAttribute('aria-invalid');
        });
      });

      document.addEventListener('click', (e) => {
        if (!dropdown.contains(e.target as Node)) {
          optionsPanel.classList.add('hidden');
          arrow.classList.remove('rotate-180');
          trigger.classList.remove('border-cobalt-pop');
          trigger.setAttribute('aria-expanded', 'false');
        }
      });
    }

    if (!form || !submitBtn || !submitText || !submitIcon || !submitSpinner || !formStatus) return;

    let isLoading = false;

    function setLoading(loading: boolean) {
      isLoading = loading;
      submitBtn.disabled = loading;
      submitText.textContent = loading ? 'Надсилаю...' : 'Надіслати';
      submitIcon.classList.toggle('hidden', loading);
      submitSpinner.classList.toggle('hidden', !loading);
    }

    function showStatus(message: string, isError: boolean) {
      formStatus.textContent = message;
      formStatus.className = `text-sm text-center ${isError ? 'text-red-600' : 'text-green-600'}`;
    }

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      if (isLoading) return;

      setLoading(true);
      showStatus('', false);

      try {
        const formData = new FormData(form);
        const response = await fetch('/api/contact', {
          method: 'POST',
          body: formData,
        });

        const result = await response.json();

        if (!response.ok) {
          throw new Error(result.error || 'Помилка');
        }
        
        showStatus('Повідомлення надіслано! Я зв\'яжусь з вами протягом 24 годин.', false);
        form.reset();
        if (triggerContent) {
          triggerContent.innerHTML = '<span class="text-black/40">Оберіть послугу</span>';
        }
        if (hiddenInput) hiddenInput.value = '';
      } catch {
        showStatus('Помилка при надсиланні. Спробуйте ще раз або напишіть мені напряму.', true);
      } finally {
        setLoading(false);
      }
    });

    const requiredFields = form.querySelectorAll('[required]');
    requiredFields.forEach((field) => {
      field.addEventListener('blur', () => {
        const input = field as HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement;
        if (input.value.trim() === '') {
          input.setAttribute('aria-invalid', 'true');
        } else {
          input.removeAttribute('aria-invalid');
        }
      });
    });
  });
</script>
